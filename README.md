# WaspLine Reader
FOSS version of BeeLine Reader

## Build
```bash
bash build.sh
```

## Libraries used
[jsLineWrapDetector](https://github.com/xdamman/js-line-wrap-detector)

## TO-DO
Integrate Readability into the extension so the color gradients are only applied to the main text
